export const API_SUCCESS_CODE = 0;
export const UNKNOWN_ERROR = 'UKNOWN API ERROR';

export enum Mode_ENUM {
  XML =  'xml',
  HTML = 'html',
  JSON = 'json',
}

export enum Unit_ENUM {
  STANDARD = 'standard',
  METRIC = 'metric',
  IMPERIAL = 'imperial',
}